{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All Projects",
            "type": "shell",
            "command": "./buildallprojects.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "options": {
                "cwd": "${workspaceFolder}/src"
            }
        }
    ]
}